import SwiftUI
import libdlkit

struct ContentView: View {
    var body: some View {
        Text("Hello, DlKit!")
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
